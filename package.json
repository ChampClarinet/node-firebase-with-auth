{
  "license": "MIT",
  "dependencies": {
    "@google-cloud/storage": "^5.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "multer": "^1.4.2"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/multer": "^1.4.5",
    "tsc": "^2.0.3",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "build": "tsc ./src/index.ts --outDir ./dist",
    "start": "yarn build && node ./dist"
  }
}
